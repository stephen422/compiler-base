fn main() -> int {
    let letb = 4
    var a: int
    a * b + 2
    b = 3

    // This is a line comment.
    let c = 3
    var d = 4.3 / 3.1415926535
    // return a
    return 3
}

fn swap(a: &int, b: int) -> int {
    let t = *a
    *a = *b
    *b = t
}

// fn parser_free(&self) -> own *T {
//     for var i = 0; i < 5; i++ {
//         next(lexer);
//     }
// }
