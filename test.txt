1 * 2 + 3 + 4 * 5;
3 + (a - (b + a * 3)) * 2;

let a;
var b = 3;

fn main() -> int {
    // This is a line comment.
    let a = 5;
    var b = 3 / 4;
    var s = "literal \"with inside string\"";
    return 0;
}
