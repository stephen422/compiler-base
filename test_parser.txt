fn main() {
    let a = 3 + 4
    let b: int
    let c &int // [error: "expected.*':'"]
    let c: int blah // [error: "expected end of decl"]

    b a // [error: "expected.*="]
    b = a

    1 * 2 + 3 + 4 * 5
    3 + (a - (b + a * 3)) * 2

    c = &b

}

fn f(a: int) int { // [error: "expected.*'->'"]
}

fn f(a: int, b int, c: int) -> int { // [error: "expected.*':'"]
}

struct Car // [error: "expected.*\{"]
    a: i64,
    b: int
    c: int, // comma is permitted
// [error: "unterminated struct"]
