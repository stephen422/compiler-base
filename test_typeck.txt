// TODO:
// struct member check

struct Car {
    engine: int,
    wheel: int,
}

func main() {
    let car: Car
    car = 3 // [error: "cannot assign.*'int'.*'Car']
    3.engine // [error: "not a structure"]
    car.wing // [error: "'wing'.*not.*member.*'Car']
    car.engine

    car.engine = car

    (3 + car)
    (3 + car).wheel
    (car).wheel
}
