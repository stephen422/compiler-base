// TODO:
// check function returns

struct Car {
    engine: int,
    wheel: int,
    engine: int, // [error: "redefinition.*'engine'"]
}

func main() {
    let a: int = 3 + 4
    let a = 4 // [error: "redefinition.*'a'"]
    let car: Car
    a = "hi"
    car.a = 5
    (car).a = 5
    (3 + 4).a = 5

    new_kid // [error: "undeclared.*'new_kid'"]

    car(3) // [error: "'car'.*not a function]
    func_in_the_back(hi, a)

    *a // [error: "dereference"] // TODO: report 'int'
}

func func_in_the_back(a: int, b: int) {
    return a + b
}
