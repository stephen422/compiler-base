// TODO:
// check function returns

struct Car {
    a: int,
    wheel: int,
    a: int, // [error: "redefinition.*'a'"]
}

fn main() {
    let a: int = 3 + 4
    let a = 4 // [error: "redefinition.*'a'"]
    let car: Car
    a = "hi"
    car.a = 5
    (car).a = 5
    (3 + 4).a = 5

    new_kid // [error: "undeclared.*'new_kid'"]

    f(hi, a)

    *a // [error: "dereference"] // TODO: report 'int'
}
