// TODO:

struct Car {
}

struct Car {
}

fn main() {
    let a: int = 3 + 4
    let a = 4 // [error: "redefinition.*'a'"]
    let b: Car // [error: "unknown type.*'Car'"]
    a = "hi"
    c = 5 // [error: "undeclared.*'c'"]

    *a // [error: "dereference"] // TODO: report 'int'
}
