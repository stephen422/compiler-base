// TODO:
// function return check -> needs control flow analysis

struct Car {
    engine: int,
    wheel: int,
    engine: int, // [error: "redefinition.*'engine'"]
}

func front_func(a: int, b: int, c: int) -> int {
}

func main() {
    let a: int = 3 + 4
    let a = 4 // [error: "redefinition.*'a'"]
    let car: Car
    a = "hi"
    car.a = 5
    (car).a = 5
    (3 + 4).a = 5

    new_kid // [error: "undeclared.*'new_kid'"]

    car(3) // [error: "'car'.*not a function]
    front_func(1 + 2)
    back_func(a, a) // TODO: multi-pass name binding

    *a // [error: "dereference"] // TODO: report 'int'
}

func back_func(a: int, b: int) {
    return a + b
}
